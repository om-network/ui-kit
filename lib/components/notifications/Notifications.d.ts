import * as React from "react";
export interface NotificationsProps {
    children: React.ReactNode;
}
export declare const Notifications: ({ children }: NotificationsProps) => JSX.Element;
export default Notifications;
